# Session Notes for session-defaults

- User prefers to create the project folder manually, then run setup.sh via curl | bash inside it.
- The setup script should use the folder name as the project name.
- After setup, user wants to run 'start session'.
- User wants tips and session memory tracked for this workflow.
